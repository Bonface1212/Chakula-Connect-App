import 'package:flutter/material.dart';

class ClaimsTab extends StatelessWidget {
  const ClaimsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("My Claimed Items"),
    );
  }
}
